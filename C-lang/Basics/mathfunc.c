#include<stdio.h>
#include<math.h>
int main()
{
                                                /* These are some more MATH related functions */
    
    double A = sqrt(9);         // Give Square root of given number.
    double B = pow(2,4);        // Gives Power to the First Entered digit.
    int C = round(3.14);        // Rounds UP the given value.
    int D = ceil(3.14);         // Ceils up i.e gives larger round up value.
    int E = floor(3.99);        // Floors up i.e gives smaller round up value.
    double F = fabs(-100);      // Gives absolute value of given number.
    double G = log(3);          // Gives the logarithm of the given value.
    double H = sin(45);         // Gives Trignometric values
    double I = cos(45);         // ---||---
    double j = tan(45);         // ---||---

    printf("\n%d",D);

    return 0;
}