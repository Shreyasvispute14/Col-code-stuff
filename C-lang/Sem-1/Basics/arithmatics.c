#include<stdio.h>
int main()
{
    // arithmetic operators
    // + (Addition)
    // - (Subtraction)
    // * (Multiplication)
    // / (Division)
    // % (Modulus)
    // ++ (Increment)
    // -- (Decrement)

    int x = 5;
    int y = 2;

    // int z1 = x+y;
    // int z2 = x-y;
    // int z3 = x*y;
    // float z4 = x / (float) y;  // For decimal point deivision value can also use (double) //
    // int z5 = x % y;
    //x++;
    //y--;
    
    printf("%d",x,y);

    return 0;
}